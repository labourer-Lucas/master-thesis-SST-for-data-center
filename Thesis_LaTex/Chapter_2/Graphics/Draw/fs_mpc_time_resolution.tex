\begin{tikzpicture}[scale = 1]
%Definitions
\def \distance {0.8}
\def \ref {2.5}

%Coordinate system
\draw[-triangle 45] (0, -0.1) -- (0, 2*\ref+0.5) node[above] {$y$};
\draw[-triangle 45] (-0.1, 0) -- (16*\distance+0.75, 0) node[right] {$\frac{t}{T_\text{s}}$};

%Helper lines
\foreach \point in {1, ..., 16}
{
	\draw[loosely dotted] (\distance*\point, -0.1) -- (\distance*\point, 2*\ref);
	\draw[-] (\distance*\point, 0.1) -- (\distance*\point, -0.1);
}

%x axis numbering
\foreach \point in {0, 5, ..., 15}
{
	\node[below] at (\distance*\point, -0.1) {$\point$};
}

%PWM ripple
\draw[-, color = blue] (0, \ref) \foreach \x in {0, 2, ..., 15} {-- (\x*\distance + \distance/6, \ref+1/3*2) -- (\x*\distance + 11*\distance/6, \ref-1/3*2)} -- (16*\distance + \distance/6, \ref+1/3*2);

%FS-MPC ripple
\draw[-, color = red, name path=fs_mpc] (0*\distance, \ref-2) -- (1*\distance, \ref+2) -- (6*\distance, \ref-2) -- (7*\distance, \ref+2)-- (12*\distance, \ref-2) -- (13*\distance, \ref+2) -- (16*\distance+0.167, \ref+2 - 5/6*3.167*\distance);

%Label FS-MPC
\path [name path=x_second] (6.1*\distance, 0) -- (6.1*\distance, \ref);
\draw[-triangle 45, name intersections={of=fs_mpc and x_second, by=conn_fs_mpc}] (7.5*\distance, \ref-2) node[right, fill = white] {FS-MPC} -- (conn_fs_mpc);

%Label PWM
\draw[-triangle 45] (4*\distance + 1/6*\distance, \ref+1/3*2+0.75) node[above, fill = white] {PWM}-- (4*\distance + 1/6*\distance, \ref+1/3*2);

%Reference value
\draw[-] (-0.1, \ref) node[left] {$y^*$} -- (16.167*\distance, \ref);
\end{tikzpicture}